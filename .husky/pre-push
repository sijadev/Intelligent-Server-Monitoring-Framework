#!/usr/bin/env bash
set -euo pipefail

if [[ "${SKIP_PREPUSH:-}" == "1" ]]; then
	echo "[pre-push] Skipped via SKIP_PREPUSH=1"
	exit 0
fi

echo "[pre-push] Node $(node -v)"
echo "[pre-push] No checks configured (ESLint/TypeScript/Prettier removed). Proceeding..."

